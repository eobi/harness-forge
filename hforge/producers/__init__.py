"""Producers. They PROPOSE plans in IR; they never certify one.

Replaceable by design: the generator race in the literature turns over roughly every twelve
months, so the engine grades racers rather than entering the race.
"""
