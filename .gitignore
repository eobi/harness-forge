# Python
__pycache__/
*.pyc
.pytest_cache/

# Build and campaign artifacts. Regenerated by `hforge build`; never source.
build/
corpora/
*.mgc

# Crash reproducers from local campaigns. These are unreviewed fuzzer inputs, not
# source, and an untriaged crash input does not belong in a public repository.
crash-*
Crash_*.java
timeout-*
leak-*
oom-*
