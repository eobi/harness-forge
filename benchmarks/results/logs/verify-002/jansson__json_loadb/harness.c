/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : jansson_json_loadb
 *   target  : jansson
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "jansson.h"
#include "jansson.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    json_error_t hf_r_out_error;
    int hf_ok_out_error = 0;
    json_t * hf_r_ret_json_t = NULL;
    uint8_t hf_s_mode_flags = 0;
    const uint8_t *hf_s_buffer = NULL;
    size_t hf_len_buffer = 0;

    if (hf_size > 65536u) return 0;

    size_t hf_cursor = 0;
    if (hf_cursor + 1 > hf_size) goto hf_cleanup;
    hf_s_mode_flags = hf_data[hf_cursor];
    hf_cursor += 1;
    hf_len_buffer = hf_size - hf_cursor;
    if (hf_len_buffer < 1u) goto hf_cleanup;
    hf_s_buffer = hf_data + hf_cursor;
    hf_cursor += hf_len_buffer;

    memset(&hf_r_out_error, 0, sizeof hf_r_out_error);
    /* o_consume: json_loadb [consume] */
    hf_r_ret_json_t = json_loadb((const char *)hf_s_buffer, hf_len_buffer, hf_s_mode_flags, &hf_r_out_error);
    /* o_drop_ret_json_t: json_delete [query] */
    if (hf_r_ret_json_t) {
        json_delete(hf_r_ret_json_t);
        hf_r_ret_json_t = NULL;
    }

hf_cleanup:
    ;
    return 0;
}
