/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : yajl-ruby_yajl_parse
 *   target  : yajl-ruby
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "yajl/yajl_parse.h"
#include "yajl_parse.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    yajl_handle hf_r_h = NULL;
    unsigned char hf_r_err = NULL;
    const uint8_t *hf_s_jsonText = NULL;
    size_t hf_len_jsonText = 0;

    if (hf_size > 4096u) return 0;

    size_t hf_cursor = 0;
    hf_len_jsonText = hf_size - hf_cursor;
    if (hf_len_jsonText < 1u) goto hf_cleanup;
    hf_s_jsonText = hf_data + hf_cursor;
    hf_cursor += hf_len_jsonText;

    /* o_create: yajl_alloc [create] */
    hf_r_h = yajl_alloc(0, 0, 0);
    /* o_consume: yajl_parse [consume] */
    if (hf_r_h) {
        hf_sink += (long)yajl_parse(hf_r_h, (const unsigned char *)hf_s_jsonText, hf_len_jsonText);
    }
    /* o_finish: yajl_complete_parse [query] */
    if (hf_r_h) {
        hf_sink += (long)yajl_complete_parse(hf_r_h);
    }
    /* o_error: yajl_get_error [consume] */
    if (hf_r_h) {
        hf_r_err = yajl_get_error(hf_r_h, 0, (const unsigned char *)hf_s_jsonText, hf_len_jsonText);
    }
    /* o_error_free: yajl_free_error [consume] */
    if (hf_r_h && hf_r_err) {
        yajl_free_error(hf_r_h, hf_r_err);
        hf_r_err = NULL;
    }
    /* o_destroy: yajl_free [destroy] */
    yajl_free(hf_r_h);
    hf_r_h = NULL;
    (void)hf_sink;

hf_cleanup:
    ;
    return 0;
}
