/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : libyaml_yaml_parser_load
 *   target  : libyaml
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "yaml.h"
#include "yaml.h"

int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    yaml_parser_t hf_r_h;
    int hf_ok_h = 0;
    yaml_document_t hf_r_out_document;
    int hf_ok_out_document = 0;
    const uint8_t *hf_s_input = NULL;
    size_t hf_len_input = 0;

    if (hf_size > 4096u) return 0;

    size_t hf_cursor = 0;
    hf_len_input = hf_size - hf_cursor;
    if (hf_len_input < 1u) goto hf_cleanup;
    hf_s_input = hf_data + hf_cursor;
    hf_cursor += hf_len_input;

    memset(&hf_r_h, 0, sizeof hf_r_h);
    memset(&hf_r_out_document, 0, sizeof hf_r_out_document);
    /* o_create: yaml_parser_initialize [create] */
    hf_ok_h = (int)(yaml_parser_initialize(&hf_r_h));
    /* o_feed: yaml_parser_set_input_string [consume] */
    if (hf_ok_h) {
        yaml_parser_set_input_string(&hf_r_h, (const unsigned char *)hf_s_input, hf_len_input);
    }
    /* o_consume: yaml_parser_load [consume] */
    for (unsigned hf_it = 0; hf_it < 4096u; ++hf_it) {
    if (hf_ok_h) {
        hf_ok_out_document = (int)(yaml_parser_load(&hf_r_h, &hf_r_out_document));
        }
        if (!hf_ok_out_document) break;
        /* o_drop_out_document: yaml_document_delete (per iteration) */
        yaml_document_delete(&hf_r_out_document);
    }
    /* o_destroy: yaml_parser_delete [destroy] */
    yaml_parser_delete(&hf_r_h);
    hf_ok_h = 0;

hf_cleanup:
    ;
    return 0;
}
