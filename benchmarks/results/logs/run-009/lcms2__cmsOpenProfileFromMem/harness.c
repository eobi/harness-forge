/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : lcms2_cmsOpenProfileFromMem
 *   target  : lcms2
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "lcms2.h"
#include "lcms2.h"

int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    cmsHPROFILE hf_r_owned = NULL;
    const uint8_t *hf_s_MemPtr = NULL;
    size_t hf_len_MemPtr = 0;

    if (hf_size > 65536u) return 0;

    size_t hf_cursor = 0;
    hf_len_MemPtr = hf_size - hf_cursor;
    if (hf_len_MemPtr < 1u) goto hf_cleanup;
    hf_s_MemPtr = hf_data + hf_cursor;
    hf_cursor += hf_len_MemPtr;

    /* o_consume: cmsOpenProfileFromMem [consume] */
    hf_r_owned = cmsOpenProfileFromMem((const void *)hf_s_MemPtr, hf_len_MemPtr);
    /* o_free_owned: cmsCloseProfile [query] */
    if (hf_r_owned) {
        hf_sink += (long)cmsCloseProfile(hf_r_owned);
        hf_r_owned = NULL;
    }
    (void)hf_sink;

hf_cleanup:
    ;
    return 0;
}
