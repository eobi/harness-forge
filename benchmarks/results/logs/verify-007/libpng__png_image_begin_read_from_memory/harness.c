/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : libpng_png_image_begin_read_from_memory
 *   target  : libpng
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "png.h"
#include "pngconf.h"
#include "png.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    png_structp hf_r_h = NULL;
    png_image hf_r_out_image;
    int hf_ok_out_image = 0;
    const uint8_t *hf_s_memory = NULL;
    size_t hf_len_memory = 0;

    if (hf_size > 65536u) return 0;

    size_t hf_cursor = 0;
    hf_len_memory = hf_size - hf_cursor;
    if (hf_len_memory < 1u) goto hf_cleanup;
    hf_s_memory = hf_data + hf_cursor;
    hf_cursor += hf_len_memory;

    memset(&hf_r_out_image, 0, sizeof hf_r_out_image);
    /* o_create: png_create_read_struct [create] */
    hf_r_h = png_create_read_struct(0, 0, 0, 0);
    /* o_consume: png_image_begin_read_from_memory [consume] */
    for (unsigned hf_it = 0; hf_it < 65536u; ++hf_it) {
        hf_ok_out_image = (int)(png_image_begin_read_from_memory(&hf_r_out_image, (png_const_voidp)hf_s_memory, hf_len_memory));
        if (!hf_ok_out_image) break;
        /* o_drop_out_image: png_image_free (per iteration) */
        png_image_free(&hf_r_out_image);
    }
    /* o_destroy: png_write_chunk_end [destroy] */
    png_write_chunk_end(hf_r_h);
    hf_r_h = NULL;

hf_cleanup:
    ;
    return 0;
}
