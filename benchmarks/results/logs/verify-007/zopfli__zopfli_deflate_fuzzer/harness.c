/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : zopfli_ZopfliDeflate
 *   target  : zopfli
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "deflate.h"
#include "zopfli.h"
#include "deflate.h"
#include "zopfli.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    ZopfliOptions hf_r_cfg_options;
    int hf_ok_cfg_options = 0;
    uint8_t hf_s_mode_final = 0;
    uint8_t hf_s_mode_btype = 0;
    const uint8_t *hf_s_in = NULL;
    size_t hf_len_in = 0;
    static unsigned char hf_sc_outbuf_bp[65536];
    unsigned char * hf_sc_own_out = NULL;
    size_t hf_sc_len_outsize = 0;

    if (hf_size > 4096u) return 0;

    size_t hf_cursor = 0;
    if (hf_cursor + 1 > hf_size) goto hf_cleanup;
    hf_s_mode_final = hf_data[hf_cursor];
    hf_cursor += 1;
    if (hf_cursor + 1 > hf_size) goto hf_cleanup;
    hf_s_mode_btype = hf_data[hf_cursor];
    hf_cursor += 1;
    hf_len_in = hf_size - hf_cursor;
    if (hf_len_in < 1u) goto hf_cleanup;
    hf_s_in = hf_data + hf_cursor;
    hf_cursor += hf_len_in;
    memset(hf_sc_outbuf_bp, 0, sizeof hf_sc_outbuf_bp);
    hf_sc_len_outsize = (size_t)(0);

    memset(&hf_r_cfg_options, 0, sizeof hf_r_cfg_options);
    /* o_cfg_options: ZopfliInitOptions [query] */
    ZopfliInitOptions(&hf_r_cfg_options);
    hf_ok_cfg_options = 1;
    /* o_consume: ZopfliDeflate [query] */
    ZopfliDeflate(&hf_r_cfg_options, hf_s_mode_btype, hf_s_mode_final, (const unsigned char*)hf_s_in, hf_len_in, (unsigned char*)hf_sc_outbuf_bp, &hf_sc_own_out, &hf_sc_len_outsize);

hf_cleanup:
    free((void *)hf_sc_own_out);
    return 0;
}
