/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : zstd_ZSTD_decompress
 *   target  : zstd
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "zstd.h"
#include "zstd.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    const uint8_t *hf_s_dst = NULL;
    size_t hf_len_dst = 0;

    if (hf_size > 65536u) return 0;

    size_t hf_cursor = 0;
    hf_len_dst = hf_size - hf_cursor;
    if (hf_len_dst < 1u) goto hf_cleanup;
    hf_s_dst = hf_data + hf_cursor;
    hf_cursor += hf_len_dst;

    /* o_consume: ZSTD_decompress [consume] */
    hf_sink += (long)ZSTD_decompress((void*)hf_s_dst, hf_len_dst, 0, 0);
    (void)hf_sink;

hf_cleanup:
    ;
    return 0;
}
