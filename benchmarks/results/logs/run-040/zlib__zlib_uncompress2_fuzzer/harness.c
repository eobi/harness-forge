/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : zlib_uncompress2
 *   target  : zlib
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "zlib.h"
#include "zlib.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    const uint8_t *hf_s_source = NULL;
    size_t hf_len_source = 0;
    static unsigned char hf_sc_outbuf_dest[65536];
    uLongf hf_sc_len_destLen = 0;
    uLong hf_sc_len_sourceLen = 0;

    if (hf_size > 4096u) return 0;

    size_t hf_cursor = 0;
    hf_len_source = hf_size - hf_cursor;
    if (hf_len_source < 1u) goto hf_cleanup;
    hf_s_source = hf_data + hf_cursor;
    hf_cursor += hf_len_source;
    memset(hf_sc_outbuf_dest, 0, sizeof hf_sc_outbuf_dest);
    hf_sc_len_destLen = (uLongf)(65536);
    hf_sc_len_sourceLen = (uLong)(hf_len_source);

    /* o_consume: uncompress2 [query] */
    hf_sink += (long)uncompress2((Bytef *)hf_sc_outbuf_dest, &hf_sc_len_destLen, (const Bytef *)hf_s_source, &hf_sc_len_sourceLen);
    (void)hf_sink;

hf_cleanup:
    ;
    return 0;
}
