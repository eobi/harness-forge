/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness  : wabt_ReadBinaryIr
 *   target   : wabt  (C++)
 *   producer : cxx_header
 *
 * `extern "C"` on the entry point is not decoration: libFuzzer looks up an UNMANGLED symbol,
 * and without it the harness compiles cleanly and the fuzzer never finds it.
 */
#include <cstdint>
#include <cstddef>
#include <cstring>
#include <algorithm>
#include <optional>
#include <string>
#include <vector>

#include "/b/wabt/include/wabt/binary-reader-ir.h"
#include "/b/wabt/include/wabt/error.h"
#include "/b/wabt/include/wabt/binary-reader.h"
#include "/b/wabt/include/wabt/ir.h"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    std::string hf_s_d(reinterpret_cast<const char *>(hf_data), hf_size);
    std::vector<uint8_t> hf_s_d_v(hf_s_d.begin(), hf_s_d.end());
    std::vector<wabt::Error> hf_x_b4{};
    std::optional<wabt::ReadBinaryOptions> hf_o_a3;
    std::optional<wabt::Module> hf_o_a5;

    if (hf_size > 65536u) return 0;

    /* o_a3: wabt::ReadBinaryOptions::ReadBinaryOptions [create] */
    hf_o_a3.emplace();
    /* o_a5: wabt::Module::Module [create] */
    hf_o_a5.emplace();
    /* o_consume: wabt::ReadBinaryIr [consume] */
    hf_sink += (long)wabt::ReadBinaryIr("", reinterpret_cast<const void*>(hf_s_d.data()), hf_s_d.size(), *hf_o_a3, &hf_x_b4, &*hf_o_a5);
    (void)hf_sink;

    return 0;
}
