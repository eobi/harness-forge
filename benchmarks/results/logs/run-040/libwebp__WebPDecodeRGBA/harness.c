/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : libwebp_WebPDecodeRGBA
 *   target  : libwebp
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "webp/decode.h"
#include "webp/types.h"
#include "webp/decode.h"
#include "webp/types.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    uint8_t* hf_r_owned = NULL;
    const uint8_t *hf_s_data = NULL;
    size_t hf_len_data = 0;
    int hf_sc_len_width = 0;
    int hf_sc_len_height = 0;

    if (hf_size > 65536u) return 0;

    size_t hf_cursor = 0;
    hf_len_data = hf_size - hf_cursor;
    if (hf_len_data < 1u) goto hf_cleanup;
    hf_s_data = hf_data + hf_cursor;
    hf_cursor += hf_len_data;
    hf_sc_len_width = (int)(0);
    hf_sc_len_height = (int)(0);

    /* o_consume: WebPDecodeRGBA [consume] */
    hf_r_owned = WebPDecodeRGBA(hf_s_data, hf_len_data, &hf_sc_len_width, &hf_sc_len_height);
    /* o_free_owned: WebPFree [query] */
    if (hf_r_owned) {
        WebPFree(hf_r_owned);
        hf_r_owned = NULL;
    }

hf_cleanup:
    ;
    return 0;
}
