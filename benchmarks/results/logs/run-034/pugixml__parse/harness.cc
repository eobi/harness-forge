/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness  : pugixml_xml_document_load_buffer
 *   target   : pugixml  (C++)
 *   producer : cxx_header
 *
 * `extern "C"` on the entry point is not decoration: libFuzzer looks up an UNMANGLED symbol,
 * and without it the harness compiles cleanly and the fuzzer never finds it.
 */
#include <cstdint>
#include <cstddef>
#include <cstring>
#include <algorithm>
#include <optional>
#include <string>
#include <vector>

#include "/b/pugixml/src/pugixml.hpp"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    std::string hf_s_d(reinterpret_cast<const char *>(hf_data), hf_size);
    std::vector<uint8_t> hf_s_d_v(hf_s_d.begin(), hf_s_d.end());
    std::optional<pugi::xml_document> hf_o_o;

    if (hf_size > 4096u) return 0;

    /* o_new: pugi::xml_document::xml_document [create] */
    hf_o_o.emplace();
    /* o_consume_0: pugi::xml_document::load_buffer [consume] */
    if (hf_o_o) {
        hf_sink += reinterpret_cast<intptr_t>((void *)&hf_o_o);
        hf_o_o->load_buffer(reinterpret_cast<const void*>(hf_s_d.data()), hf_s_d.size(), pugi::parse_minimal);
    }
    /* o_consume_1: pugi::xml_document::load_buffer [consume] */
    if (hf_o_o) {
        hf_sink += reinterpret_cast<intptr_t>((void *)&hf_o_o);
        hf_o_o->load_buffer(reinterpret_cast<const void*>(hf_s_d.data()), hf_s_d.size(), pugi::parse_default);
    }
    /* o_consume_2: pugi::xml_document::load_buffer [consume] */
    if (hf_o_o) {
        hf_sink += reinterpret_cast<intptr_t>((void *)&hf_o_o);
        hf_o_o->load_buffer(reinterpret_cast<const void*>(hf_s_d.data()), hf_s_d.size(), pugi::parse_full);
    }
    (void)hf_sink;

    return 0;
}
