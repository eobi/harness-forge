/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : libde265_de265_push_data
 *   target  : libde265
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "de265.h"
#include "de265.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    const uint8_t *hf_s_data = NULL;
    size_t hf_len_data = 0;
    static unsigned char hf_sc_outbuf_arg0[1048576];
    static unsigned char hf_sc_outbuf_user_data[1048576];

    if (hf_size > 65536u) return 0;

    size_t hf_cursor = 0;
    hf_len_data = hf_size - hf_cursor;
    if (hf_len_data < 1u) goto hf_cleanup;
    hf_s_data = hf_data + hf_cursor;
    hf_cursor += hf_len_data;
    memset(hf_sc_outbuf_arg0, 0, sizeof hf_sc_outbuf_arg0);
    memset(hf_sc_outbuf_user_data, 0, sizeof hf_sc_outbuf_user_data);

    /* o_consume: de265_push_data [query] */
    hf_sink += (long)de265_push_data((de265_decoder_context*)hf_sc_outbuf_arg0, (const void*)hf_s_data, hf_len_data, 0, (void*)hf_sc_outbuf_user_data);
    (void)hf_sink;

hf_cleanup:
    ;
    return 0;
}
