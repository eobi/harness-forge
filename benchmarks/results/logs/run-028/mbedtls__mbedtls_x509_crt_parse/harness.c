/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : mbedtls_mbedtls_x509_crt_parse
 *   target  : mbedtls
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "mbedtls/x509_crt.h"
#include "mbedtls/x509_crt.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    mbedtls_x509_crt hf_r_h;
    int hf_ok_h = 0;
    const uint8_t *hf_s_buf = NULL;
    size_t hf_len_buf = 0;

    if (hf_size > 65536u) return 0;

    size_t hf_cursor = 0;
    hf_len_buf = hf_size - hf_cursor;
    if (hf_len_buf < 1u) goto hf_cleanup;
    hf_s_buf = hf_data + hf_cursor;
    hf_cursor += hf_len_buf;

    memset(&hf_r_h, 0, sizeof hf_r_h);
    /* o_create: mbedtls_x509_crt_init [create] */
    mbedtls_x509_crt_init(&hf_r_h);
    hf_ok_h = 1;
    /* o_consume: mbedtls_x509_crt_parse [consume] */
    if (hf_ok_h) {
        hf_sink += (long)mbedtls_x509_crt_parse(&hf_r_h, (const unsigned char *)hf_s_buf, hf_len_buf);
    }
    /* o_destroy: mbedtls_x509_crt_free [destroy] */
    mbedtls_x509_crt_free(&hf_r_h);
    hf_ok_h = 0;
    (void)hf_sink;

hf_cleanup:
    ;
    return 0;
}
