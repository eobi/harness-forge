/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : zopfli_ZopfliDeflate
 *   target  : zopfli
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "deflate.h"
#include "zopfli.h"
#include "deflate.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    ZopfliOptions hf_r_out_options;
    int hf_ok_out_options = 0;
    const uint8_t *hf_s_in = NULL;
    size_t hf_len_in = 0;

    if (hf_size > 4096u) return 0;

    size_t hf_cursor = 0;
    hf_len_in = hf_size - hf_cursor;
    if (hf_len_in < 1u) goto hf_cleanup;
    hf_s_in = hf_data + hf_cursor;
    hf_cursor += hf_len_in;

    memset(&hf_r_out_options, 0, sizeof hf_r_out_options);
    /* o_consume: ZopfliDeflate [consume] */
    ZopfliDeflate(&hf_r_out_options, 0, 0, (const unsigned char*)hf_s_in, hf_len_in, 0, 0, 0);
    hf_ok_out_options = 1;

hf_cleanup:
    ;
    return 0;
}
