/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : brotli_BrotliDecoderDecompressStream
 *   target  : brotli
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "brotli/decode.h"
#include "brotli/decode.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    BrotliDecoderState* hf_r_h = NULL;
    const uint8_t *hf_s_next_in_in = NULL;
    size_t hf_len_next_in_in = 0;
    const uint8_t * hf_sc_cur_next_in = NULL;
    static unsigned char hf_sc_outbuf_next_out[65536];
    uint8_t * hf_sc_cur_next_out = NULL;
    size_t hf_sc_len_available_in = 0;
    size_t hf_sc_len_available_out = 0;
    size_t hf_sc_len_total_out = 0;

    if (hf_size > 4096u) return 0;

    size_t hf_cursor = 0;
    hf_len_next_in_in = hf_size - hf_cursor;
    if (hf_len_next_in_in < 1u) goto hf_cleanup;
    hf_s_next_in_in = hf_data + hf_cursor;
    hf_cursor += hf_len_next_in_in;
    hf_sc_cur_next_in = (const uint8_t *)hf_s_next_in_in;
    memset(hf_sc_outbuf_next_out, 0, sizeof hf_sc_outbuf_next_out);
    hf_sc_cur_next_out = (uint8_t *)hf_sc_outbuf_next_out;
    hf_sc_len_available_in = (size_t)(hf_len_next_in_in);
    hf_sc_len_available_out = (size_t)(65536);
    hf_sc_len_total_out = (size_t)(65536);

    /* o_create: BrotliDecoderCreateInstance [create] */
    hf_r_h = BrotliDecoderCreateInstance(0, 0, 0);
    /* o_consume: BrotliDecoderDecompressStream [consume] */
    for (unsigned hf_it = 0; hf_it < 4096u; ++hf_it) {
    if (hf_r_h) {
        hf_sink += (long)(BrotliDecoderDecompressStream(hf_r_h, &hf_sc_len_available_in, &hf_sc_cur_next_in, &hf_sc_len_available_out, &hf_sc_cur_next_out, &hf_sc_len_total_out));
        if (!hf_sink) break;
        }
    }
    /* o_finish: BrotliDecoderIsFinished [query] */
    if (hf_r_h) {
        hf_sink += (long)BrotliDecoderIsFinished(hf_r_h);
    }
    /* o_destroy: BrotliDecoderDestroyInstance [destroy] */
    BrotliDecoderDestroyInstance(hf_r_h);
    hf_r_h = NULL;
    (void)hf_sink;

hf_cleanup:
    ;
    return 0;
}
