/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : jbig2dec_jbig2_data_in
 *   target  : jbig2dec
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "jbig2.h"
#include "jbig2.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    Jbig2Ctx * hf_r_h = NULL;
    const uint8_t *hf_s_data = NULL;
    size_t hf_len_data = 0;

    if (hf_size > 65536u) return 0;

    size_t hf_cursor = 0;
    hf_len_data = hf_size - hf_cursor;
    if (hf_len_data < 1u) goto hf_cleanup;
    hf_s_data = hf_data + hf_cursor;
    hf_cursor += hf_len_data;

    /* o_create: jbig2_ctx_new_imp [create] */
    hf_r_h = jbig2_ctx_new_imp(0, 0, 0, 0, 0, 0, 20);
    /* o_consume: jbig2_data_in [consume] */
    if (hf_r_h) {
        hf_sink += (long)jbig2_data_in(hf_r_h, (const unsigned char *)hf_s_data, hf_len_data);
    }
    /* o_finish: jbig2_complete_page [query] */
    if (hf_r_h) {
        hf_sink += (long)jbig2_complete_page(hf_r_h);
    }
    /* o_destroy: jbig2_ctx_free [destroy] */
    hf_sink += (long)jbig2_ctx_free(hf_r_h);
    hf_r_h = NULL;
    (void)hf_sink;

hf_cleanup:
    ;
    return 0;
}
