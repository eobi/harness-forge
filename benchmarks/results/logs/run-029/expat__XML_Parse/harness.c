/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : expat_XML_Parse
 *   target  : expat
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "expat.h"
#include "expat.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    XML_Parser hf_r_h = NULL;
    const uint8_t *hf_s_s = NULL;
    size_t hf_len_s = 0;

    if (hf_size > 65536u) return 0;

    size_t hf_cursor = 0;
    hf_len_s = hf_size - hf_cursor;
    if (hf_len_s < 1u) goto hf_cleanup;
    hf_s_s = hf_data + hf_cursor;
    hf_cursor += hf_len_s;

    /* o_create: XML_ParserCreate [create] */
    hf_r_h = XML_ParserCreate(0);
    /* o_consume: XML_Parse [consume] */
    if (hf_r_h) {
        hf_sink += (long)XML_Parse(hf_r_h, (const char *)hf_s_s, hf_len_s, 0);
    }
    /* o_destroy: XML_ParserFree [destroy] */
    XML_ParserFree(hf_r_h);
    hf_r_h = NULL;
    (void)hf_sink;

hf_cleanup:
    ;
    return 0;
}
