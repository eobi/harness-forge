/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : iperf_cJSON_Compare
 *   target  : iperf
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "cJSON.h"
#include "cJSON.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    cJSON * hf_r_h = NULL;
    char *hf_s_value = NULL;
    size_t hf_len_value = 0;

    if (hf_size > 4096u) return 0;

    size_t hf_cursor = 0;
    hf_len_value = hf_size - hf_cursor;
    if (hf_len_value < 1u) goto hf_cleanup;
    hf_s_value = (char *)malloc(hf_len_value + 1);
    if (!hf_s_value) goto hf_cleanup;
    memcpy(hf_s_value, hf_data + hf_cursor, hf_len_value);
    hf_s_value[hf_len_value] = '\0';   /* contract: NUL-terminated */
    hf_cursor += hf_len_value;

    /* o_create: cJSON_Parse [create] */
    hf_r_h = cJSON_Parse(hf_s_value);
    /* o_consume: cJSON_Compare [consume] */
    if (hf_r_h) {
        hf_sink += (long)cJSON_Compare(hf_r_h, hf_r_h, 0);
    }
    /* o_destroy: cJSON_Delete [destroy] */
    cJSON_Delete(hf_r_h);
    hf_r_h = NULL;
    (void)hf_sink;

hf_cleanup:
    free(hf_s_value);
    return 0;
}
