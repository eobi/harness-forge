/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness : libde265_de265_push_data
 *   target  : libde265
 *   producer: header_graph
 *   platforms: linux-aarch64-glibc
 *
 * Do not edit this file. Edit the IR and re-emit, or the certificate stops describing
 * what you are actually running.
 */

#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>

#include "de265.h"
#include "de265.h"

#ifdef __cplusplus
extern "C"
#endif
int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    de265_decoder_context* hf_r_h = NULL;
    int hf_out_o_drive_more = {0};
    const uint8_t *hf_s_data = NULL;
    size_t hf_len_data = 0;

    if (hf_size > 65536u) return 0;

    size_t hf_cursor = 0;
    hf_len_data = hf_size - hf_cursor;
    if (hf_len_data < 1u) goto hf_cleanup;
    hf_s_data = hf_data + hf_cursor;
    hf_cursor += hf_len_data;

    /* o_create: de265_new_decoder [create] */
    hf_r_h = de265_new_decoder();
    /* o_consume: de265_push_data [consume] */
    if (hf_r_h) {
        hf_sink += (long)de265_push_data(hf_r_h, (const void*)hf_s_data, hf_len_data, 0, 0);
    }
    /* o_finish: de265_flush_data [query] */
    if (hf_r_h) {
        hf_sink += (long)de265_flush_data(hf_r_h);
    }
    /* o_drive: de265_decode [consume] */
    if (hf_r_h) {
        hf_sink += (long)de265_decode(hf_r_h, &hf_out_o_drive_more);
    }
    /* o_destroy: de265_free_decoder [destroy] */
    hf_sink += (long)de265_free_decoder(hf_r_h);
    hf_r_h = NULL;
    (void)hf_sink;

hf_cleanup:
    ;
    return 0;
}
