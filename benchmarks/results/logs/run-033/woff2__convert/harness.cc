/* Generated by Harness Forge from a certified Harness IR plan.
 *
 *   harness  : woff2_ConvertWOFF2ToTTF
 *   target   : woff2  (C++)
 *   producer : cxx_header
 *
 * `extern "C"` on the entry point is not decoration: libFuzzer looks up an UNMANGLED symbol,
 * and without it the harness compiles cleanly and the fuzzer never finds it.
 */
#include <cstdint>
#include <cstddef>
#include <cstring>
#include <algorithm>
#include <optional>
#include <string>
#include <vector>

#include "/b/woff2/include/woff2/decode.h"
#include "/b/woff2/include/woff2/output.h"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *hf_data, size_t hf_size) {
    volatile long hf_sink = 0;
    std::string hf_s_d(reinterpret_cast<const char *>(hf_data), hf_size);
    std::vector<uint8_t> hf_s_d_v(hf_s_d.begin(), hf_s_d.end());
    std::string hf_x_b2{};
    std::optional<woff2::WOFF2StringOut> hf_o_a2;

    if (hf_size > 65536u) return 0;

    /* o_a2: woff2::WOFF2StringOut::WOFF2StringOut [create] */
    hf_o_a2.emplace(&hf_x_b2);
    /* o_consume: woff2::ConvertWOFF2ToTTF [consume] */
    hf_sink += (long)woff2::ConvertWOFF2ToTTF(reinterpret_cast<const uint8_t *>(hf_s_d.data()), hf_s_d.size(), &*hf_o_a2);
    (void)hf_sink;

    return 0;
}
