{
 "date": "2026-09-01",
 "what": "Status of coverage-guided GUI campaigns: the capability works, the loop integration does not yet, and two environment facts were found that invalidate a comparison we had been making.",
 "WORKS_verified_twice": {
  "eog_application_coverage": "25.64% of 78 regions from a single image open",
  "libpng_decoder_coverage": "12.10% of 18,400 regions from a single image open",
  "why_the_second_matters": "eog's own binary has 78 regions; libpng has 18,400. Application coverage was 236x coarser and measured the wrong surface -- a corrupt PNG is handled in the decoder, not in the viewer's UI code.",
  "mechanism": "instrumented build -> open file -> wait for quiescence -> xdotool ctrl+q/ctrl+w for a CLEAN EXIT -> .profraw -> llvm-profdata -> llvm-cov"
 },
 "DOES_NOT_WORK_YET": {
  "symptom": "_regions_covered() reports 'no profile written' on every input inside the campaign loop, so the corpus never grows and no guidance happens. An 87KB .profraw IS present on disk after the campaign finishes.",
  "confirmed_facts": [
   "The INSTRUMENTED binary is the one running: readlink /proc/<pid>/exe returns /home/ubuntu/covbuild/eog-42.0/build/src/eog.",
   "It exits cleanly: p.poll() returns 0 after the window close.",
   "xdotool finds 2 windows for the pid and the keys are delivered.",
   "A profile IS produced -- 87272 bytes, visible after the run.",
   "The same binary, same environment, run STANDALONE writes 639288 bytes reliably.",
   "llvm-profdata and llvm-cov both succeed on that profile and _regions_covered() returns 2235 when called by hand against it."
  ],
  "positions_the_wait_was_tried_in": [
   "between the window close and terminate(), 2s -- always empty",
   "same position, 8s -- always empty",
   "after terminate() and after the app leaves the accessibility tree, 6s -- always empty"
  ],
  "profile_naming_tried": [
   "%p (per-process)",
   "%m (merge pool)"
  ],
  "not_yet_explained": "Why a profile that exists on disk after the campaign is never visible to a read inside the loop, at any wait position tried. The next thing to check is whether the campaign's own delete-before-each-input step races the write of the PREVIOUS input, which would explain a file that is always present afterwards and never present during.",
  "attempts": 8,
  "blast_radius": "None. Everything is behind HF_GUI_COVERAGE; a blind campaign takes the identical path and was re-verified after every change."
 },
 "TWO_ENVIRONMENT_FACTS_THAT_INVALIDATE_A_COMPARISON": {
  "container_eog_and_system_eog_disagree": "With the SAME seed, the SAME rng (1337) and therefore the same mutation sequence, input 006 -- a 45-byte truncated PNG -- is ACCEPTED by the container's eog and REJECTED by the VM's system eog. The 29% acceptance from the 5000-input campaign is a CONTAINER number and must not be compared against native runs.",
  "the_seed_is_chosen_by_directory_order": "The campaign picks the first PNG over 2000 bytes from a sorted walk of /usr/share/icons. Installing five more applications added icons and could have changed it silently. It did not this time -- the seed is still ac-adapter.png -- but a benchmark whose input depends on what else is installed is not reproducible, and --seed-file should be mandatory for any recorded run."
 },
 "A_NOTE_ON_HOW_LONG_THIS_TOOK": "Eight attempts, five of them from reading the profile too early in the process lifecycle. The bare `return -1` hid the cause through the first five: a silent failure that looks like data is worse than a loud one, because -1 is indistinguishable from 'this input covered nothing' and the campaign kept running as though guided. Adding a reason to every failure path should have been the FIRST change, not the sixth."
}